{
  "name": "zauru-sdk",
  "private": true,
  "scripts": {
    "build": "pnpm -r run build",
    "clean": "pnpm -r exec rm -rf dist",
    "publish:packages": "lerna publish"
  },
  "devDependencies": {
    "lerna": "^8.1.2"
  },
  "engines": {
    "node": ">=18",
    "pnpm": ">=9"
  }
}
